spring:
  main:
    banner-mode: off
  ai:
    mcp:
      server:
        name: tictactoe-server
        version: 1.0.0
        protocol: STREAMABLE
        request-timeout: 30s

server:
  port: 8080

logging:
  level:
    "[com.example.springai]": INFO
