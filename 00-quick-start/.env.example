# 00-quick-start uses GitHub Models (not Azure OpenAI) via System.getenv("GITHUB_TOKEN").
# The recommended workflow is to set GITHUB_TOKEN in the repository-root .env file.
GITHUB_TOKEN=your-github-token-here
